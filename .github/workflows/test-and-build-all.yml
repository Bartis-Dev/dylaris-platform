name: Test and Build All (manual)

# Manual-only. Runs the FULL test suite first (test.yml), then - only if every
# test job is green - builds and pushes every service image unconditionally,
# with no change detection. Use after dependency bumps, base-image rotations,
# or to repush a verified-clean set of images.
#
# To run: GitHub Actions UI -> "Test and Build All (manual)" -> Run workflow.
# For normal pushes, test.yml runs the tests and ci.yml builds changed images.

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: bartis-dev/dylaris-platform

jobs:
  # ----------------------------------------------
  # Gate: full test suite must pass before any build
  # ----------------------------------------------
  test:
    uses: ./.github/workflows/test.yml

  # ----------------------------------------------
  # Core
  # ----------------------------------------------
  build-core:
    needs: test
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine tags
        id: tags
        run: |
          SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "TAG_PRIMARY=latest" >> $GITHUB_OUTPUT
            echo "TAG_SHA=sha-${SHA}" >> $GITHUB_OUTPUT
          else
            echo "TAG_PRIMARY=dev" >> $GITHUB_OUTPUT
            echo "TAG_SHA=dev-sha-${SHA}" >> $GITHUB_OUTPUT
          fi

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Core
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-core:${{ steps.tags.outputs.TAG_PRIMARY }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-core:${{ steps.tags.outputs.TAG_SHA }}
          build-args: |
            ENTRY_PATH=./core/
            SERVICE=core
            RUN_AS=dylaris

  # ----------------------------------------------
  # Node, Panel
  # ----------------------------------------------
  build-node:
    needs: test
    uses: ./.github/workflows/build-service.yml
    with:
      component: node
      entry_path: ./node/
      extra_build_args: "INSTALL_QUOTA=1"
    secrets: inherit

  build-panel:
    needs: test
    uses: ./.github/workflows/build-service.yml
    with:
      component: panel
      dockerfile: ./panel/Dockerfile
      context: .
      # Owner's published image keeps its baked SaaS URL (the ARG now defaults
      # to empty for a self-hoster's own build).
      extra_build_args: "NEXT_PUBLIC_API_URL=https://api.dylaris.com/api"
    secrets: inherit

  # ----------------------------------------------
  # Log-Shipper (MC server images)
  # ----------------------------------------------
  build-log-shipper:
    needs: test
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'
          cache-dependency-path: log-shipper/go.sum

      - name: Build log-shipper binary
        run: |
          chmod +x ./build_shipper.sh
          ./build_shipper.sh

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine tags
        id: tags
        run: |
          SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "TAG_PRIMARY=latest" >> $GITHUB_OUTPUT
            echo "TAG_SHA=sha-${SHA}" >> $GITHUB_OUTPUT
          else
            echo "TAG_PRIMARY=dev" >> $GITHUB_OUTPUT
            echo "TAG_SHA=dev-sha-${SHA}" >> $GITHUB_OUTPUT
          fi

      - name: Build and push java21
        uses: docker/build-push-action@v5
        with:
          context: docker/mc-server
          file: docker/mc-server/Dockerfile.java21
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java21:${{ steps.tags.outputs.TAG_PRIMARY }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java21:${{ steps.tags.outputs.TAG_SHA }}

      - name: Build and push java17
        uses: docker/build-push-action@v5
        with:
          context: docker/mc-server
          file: docker/mc-server/Dockerfile.java17
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java17:${{ steps.tags.outputs.TAG_PRIMARY }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java17:${{ steps.tags.outputs.TAG_SHA }}

      - name: Build and push java8
        uses: docker/build-push-action@v5
        with:
          context: docker/mc-server
          file: docker/mc-server/Dockerfile.java8
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java8:${{ steps.tags.outputs.TAG_PRIMARY }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mc-java8:${{ steps.tags.outputs.TAG_SHA }}
